# Testing and delivery notes

## Automated checks
- `flutter analyze`
- `flutter test test/bdd --reporter expanded`
- GitHub Actions workflow: `.github/workflows/flutter-ci.yml`

## BDD widget protocol
- Story specs:
  - `test/bdd/begruessung.feature`
  - `test/bdd/zaehlen.feature`
  - `test/bdd/zuruecksetzen.feature`
- Executed as widget tests under `test/bdd/*.dart`
- Visual proof via goldens:
  - `test/bdd/goldens/begruessung.png`
  - `test/bdd/goldens/zaehlen.png`
  - `test/bdd/goldens/zuruecksetzen.png`
- The screenshot comparison is the reproducible visual check for each test case.

## BDD trace
- Feature specs:
  - `test/bdd/begruessung.feature`
  - `test/bdd/zaehlen.feature`
  - `test/bdd/zuruecksetzen.feature`
- Host driver: `test/bdd/driver.dart`
- Widget verification: `test/widget_test.dart`

## Evidence to collect for release readiness
For every story/use case, capture and archive:
1. Test command and exit code
2. Screenshot of the running app on emulator/device
3. Screenshot or log excerpt of the executed test
4. GitHub Actions run URL
5. Built artifact URL (APK/IPA or release asset)

## Current local status
- `flutter analyze` succeeds with info-level `avoid_print` warnings in `test/bdd/driver.dart`.
- `flutter test` succeeds.
- The local emulator path is still blocked because the new `api34` AVD does not yet appear in `adb devices`.
- No `flutter drive` run has completed yet, so no emulator GUI screenshots have been collected locally.
- Fallback evidence exists as golden PNGs generated by `flutter test --update-goldens` for:
  - `test/bdd/goldens/begruessung.png`
  - `test/bdd/goldens/zaehlen.png`
  - `test/bdd/goldens/zuruecksetzen.png`

## Headless fallback proof
- The three BDD stories now run as widget tests and pass locally.
- Golden PNGs are produced and compared from the widget layer instead of from a live emulator session.
- This fallback is verifiable and repeatable on this host when the Android emulator is unstable.

## Replacement path for this machine
1. Keep the BDD stories as widget tests under `test/bdd/*.dart`.
2. Use goldens in `test/bdd/goldens/*.png` as the visual proof for each scenario.
3. Run `flutter test test/bdd --update-goldens` only when the UI changes.
4. Run `flutter test test/bdd` in CI and local verification.
5. Treat emulator-based `flutter drive` as optional future work, not the gate for this repository on this host.

## Planned release artifacts
- Android APK from GitHub Actions
- iOS unsigned build output from GitHub Actions

## Notes
This repository is intended to keep repeatable build and test steps in GitHub Actions. Local runs are only for verifying and iterating on the workflow before relying on CI.
